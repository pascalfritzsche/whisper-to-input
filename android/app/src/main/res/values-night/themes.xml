<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WhisperToInput" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="RegularImage">
        <item name="android:tint">@color/theme_keyboard_fg</item>
    </style>

    <style name="RegularText">
        <item name="android:textColor">@color/theme_keyboard_fg</item>
    </style>

    <style name="Background">
        <item name="android:background">@color/theme_keyboard_bg</item>
    </style>

    <style name="TopBar">
        <item name="android:background">@color/theme_keyboard_topbar</item>
    </style>

</resources>